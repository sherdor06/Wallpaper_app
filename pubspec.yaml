name: wallpaper_app_first
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.1+4

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.3.1   # rasmlarni keshlash
  flutter_staggered_grid_view: ^0.7.0  # chiroyli grid
  dio: ^5.4.0                     # yuklab olish
  path_provider: ^2.1.3           # fayl yo'li
  permission_handler: ^11.3.1     # (ixtiyoriy) ruxsatlar
  liquid_glass_widgets: ^0.19.1   # liquid glass UI (chrome + widgetlar)
  cupertino_native_better: ^1.5.2  # iOS native Liquid Glass (tab bar, glass menus/buttons)
  lottie: ^3.1.0                  # Lottie animatsiyalar (loading/splash/empty)
  url_launcher: ^6.3.0            # tashqi havolalar (privacy, rate, email)
  flutter_cache_manager: ^3.4.1  # keshni tozalash (Settings)
  package_info_plus: ^8.1.0       # app versiyasini pubspec'dan olish (Settings)
  firebase_core: ^4.12.0
  firebase_analytics: ^12.4.4
  firebase_crashlytics: ^5.2.5
  firebase_remote_config: ^6.5.4
  # Still pinned exactly, but the original reason is gone: the pin existed to
  # satisfy the AppLovin Yandex adapter's `AppMetricaLibraryAdapter ~> 6.5.0`,
  # and that adapter left with AppLovin. YandexMobileAds 8.4.0 now pulls the
  # adapter directly and may want a newer floor than 4.0.0 provides. Android
  # resolves this on its own (Gradle takes the highest version in range); if
  # CocoaPods ever refuses to install, bump this to 4.1.0 rather than fighting
  # the constraint — nothing pins it low any more.
  appmetrica_plugin: 4.0.0        # Yandex AppMetrica (MDH analitika + atribusiya + push)
  app_tracking_transparency: ^2.0.7
  # Yandex Mobile Ads, used directly rather than through a mediation layer.
  # The app's audience is CIS, where Yandex carries the demand; MAX was dropped
  # because its publisher account never opened. AdService keeps a
  # network-agnostic public surface, so putting mediation back later is a
  # change to that one file, not to the UI.
  yandex_mobileads: ^8.4.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1  # ikonka generatori (build-time tool)
  flutter_native_splash: ^2.4.1    # splash generatori (build-time tool)

# Ikonka konfiguratsiyasi: `dart run flutter_launcher_icons`
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
  remove_alpha_ios: true
  # Gradient image, not a flat colour: the icon's glass panel is a full-bleed
  # layer, and it must sit on the background (the foreground is parallax-shifted
  # by the launcher, so anything full-bleed there would slide out of register).
  adaptive_icon_background: "assets/icon/icon_background.png"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"

# Splash konfiguratsiyasi: `dart run flutter_native_splash:create`
flutter_native_splash:
  color: "#0E0E12"
  image: "assets/icon/splash.png"
  android_12:
    color: "#0E0E12"
    # Android 12+ draws this itself and masks it to a circle, so it needs its
    # own asset: a 1152px canvas with the artwork inside the centre 768px circle
    # (the platform's 288dp/192dp pair). Reusing the full-bleed splash.png here
    # puts content across 100% of the canvas and the mask cuts through the glyph.
    image: "assets/icon/android12_splash.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled namuna katalog — CDN sozlanmaganda yoki offline holatda fallback.
  assets:
    - assets/catalog.sample.json
    - assets/anim/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
